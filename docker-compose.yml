services:
  # ─── MongoDB ────────────────────────────────────────────────────────────────
  mongodb:
    image: mongo:7
    container_name: reinvent_mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: adminpassword
      MONGO_INITDB_DATABASE: reinventdb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
      - ./docker/mongo/init.js:/docker-entrypoint-initdb.d/init.js:ro
    networks:
      - reinvent-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ─── MailHog (local SMTP + web UI) ─────────────────────────────────────────
  mailhog:
    image: mailhog/mailhog:latest
    container_name: reinvent_mailhog
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI  →  http://localhost:8025
    networks:
      - reinvent-network

  # ─── Express API ────────────────────────────────────────────────────────────
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
      target: production
    container_name: reinvent_api
    restart: unless-stopped
    env_file:
      - ./server/.env
    environment:
      NODE_ENV: production
      PORT: 5000
      MONGODB_URI: mongodb://reinventapp:reinventpass@mongodb:27017/reinventdb
      # Override SMTP to point at the in-stack MailHog container
      SMTP_HOST: mailhog
      SMTP_PORT: "1025"
      SMTP_USER: ""
      SMTP_PASS: ""
    ports:
      - "5000:5000"
    depends_on:
      mongodb:
        condition: service_healthy
      mailhog:
        condition: service_started
    volumes:
      - api_logs:/app/logs
    networks:
      - reinvent-network

networks:
  reinvent-network:
    driver: bridge

volumes:
  mongo_data:
  api_logs:
